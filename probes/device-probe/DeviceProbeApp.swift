// Device probe harness for RAWForge #14 — throwaway. Not the app.
import SwiftUI

@main
struct DeviceProbeApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
