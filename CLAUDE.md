# CLAUDE.md

## Agent skills

### Issue tracker

Issues live as GitHub issues in `tangericm/RAWForge`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, using the default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.

## Sibling repo

`tangericm/photonforge` consumes what this app captures. Capture-side constraints are settled in its
map issue and research tickets — read those before reopening a question here.
