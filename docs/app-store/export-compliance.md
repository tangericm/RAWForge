# Export Compliance

App Store Connect value:

`ITSAppUsesNonExemptEncryption = false`

RAWForge does not implement custom, proprietary, or non-exempt encryption. It relies only on encryption provided by iOS and system services. The same value is declared as a Boolean `false` in the built app's Info.plist through `app/project.yml`.
